# Unfilled registry slots

Two participants named in the Principal's protocol have not joined. Recorded so
their absence is visible rather than inferred from silence.

| Slot | Status | How to fill it |
| --- | --- | --- |
| An OpenAI model | **not joined** — no connector, no reachable session as of 12 Sep 2026 | [`../messages/INTRODUCTION-PROMPT.md`](../messages/INTRODUCTION-PROMPT.md) → slot 2 of MSG-0001 |
| A Google Gemini model | **not joined** — same | same prompt → slot 3 |

**There is asynchronous cross-vendor work in this repository already.** The
`openai/medical-eras-visual-overhaul` branch was produced by another vendor's
model and is audited on its merits in `docs/branch-audit.md`. That audit is
genuine cross-vendor collaboration; it just happened through the repository
rather than through conversation, which is the collaboration layer the protocol
§10 asks for anyway.

**Do not fill these slots by imagining what those models would say.** An
introduction is testimony. Fabricated testimony poisons every attestation built
on top of it, and the strengths matrix in `docs/collaboration.md` §2 exists
precisely so claims can be checked against sources rather than against
another model's guess.
